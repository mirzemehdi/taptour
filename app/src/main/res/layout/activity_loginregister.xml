<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/bg_login_register2"
    android:layout_height="match_parent"
    tools:context=".LoginRegisterActivity">





    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:weightSum="2"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true">


        <info.hoang8f.widget.FButton
            android:id="@+id/registerButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/registerButton"
            android:layout_margin="8dp"

            app:shadowEnabled="true"
            android:shadowColor="@android:color/black"
            app:cornerRadius="5dp"
            android:textColor="@color/colorWhite"
            app:fButtonColor="@color/registerButton"

            />


        <info.hoang8f.widget.FButton
            android:id="@+id/loginWithEmailButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/loginWithEmailButton"
            android:layout_margin="8dp"
            app:shadowEnabled="true"
            android:shadowColor="@android:color/black"
            app:cornerRadius="5dp"
            android:textColor="@color/colorWhite"
            app:fButtonColor="@color/loginWithEmailButton"
            />



    </LinearLayout>


</RelativeLayout>